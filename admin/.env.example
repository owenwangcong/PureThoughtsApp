# 管理后台环境变量(NEXT_PUBLIC_* 在构建时内联进静态产物)。
# 不设置时代码默认指向 Supabase CLI 本地栈(http://127.0.0.1:54321 + demo anon key)。
# 生产构建:复制本文件为 .env.production.local(gitignored),填入生产实例的值。
NEXT_PUBLIC_SUPABASE_URL=https://api.pure-thoughts.com
NEXT_PUBLIC_SUPABASE_ANON_KEY=<生产 anon key>
